{:paths ["src"]
 ;; domain truth は noren 側。この repo は順序と I/O だけを持つ。
 :deps {io.github.kotoba-lang/noren {:local/root "../../kotoba-lang/noren"}
        io.github.kotoba-lang/design-quality {:local/root "../../kotoba-lang/design-quality"}
        io.github.kotoba-lang/jp-go-digital-design-system {:local/root "../../kotoba-lang/jp-go-digital-design-system"}
        io.github.kotoba-lang/html {:local/root "../../kotoba-lang/html"}
        io.github.kotoba-lang/css {:local/root "../../kotoba-lang/css"}}
 :aliases
 {;; 発見（discover）が乗っている 2 つ。**`:deps` ではなくここに置く。**
  ;; どちらも nbb 側（`sources.cljs` / `commoncrawl.live-http`）からしか使わない
  ;; のに、`:deps` に置くと JVM の classpath 解決に入ってくる —— 実測 2026-08-11、
  ;; kotobase-commoncrawl-actor の推移依存に壊れた `:local/root`（langchain-clj）が
  ;; あり、**`clojure -M:test` が classpath を組む段で落ちて 1 本も走らなかった**。
  ;; nbb の classpath は `--classpath` で明示するのでここに在っても無くても同じ。
  :nbb {:extra-deps {io.github.kotoba-lang/org-openstreetmap-overpass
                     {:local/root "../../kotoba-lang/org-openstreetmap-overpass"}
                     io.github.kotoba-lang/kotobase-commoncrawl-actor
                     {:local/root "../../kotoba-lang/kotobase-commoncrawl-actor"}}}
  :test {:extra-paths ["test"]
         :extra-deps {io.github.cognitect-labs/test-runner
                      {:git/tag "v0.5.1" :git/sha "dfb30dd"}}
         :main-opts ["-m" "cognitect.test-runner"]
         :exec-fn cognitect.test-runner.api/test}}}
